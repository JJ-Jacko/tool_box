[project]
name = "video_tools"
version = "1.0"
dependencies = [
    "pillow"
]

[project.scripts]
test-run = "video_tools.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
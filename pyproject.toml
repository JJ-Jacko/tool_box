[project]
name = "tool_box"
version = "2.0"
dependencies = [
    "pillow",
    "typed-ffmpeg"
]

[project.optional-dependencies]
dev = [
    "debugpy"
]

[project.scripts]
tool-box = "tool_box.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
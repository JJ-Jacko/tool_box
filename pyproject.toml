[project]
name = "tool_box"
version = "1.0"
dependencies = [
    "pillow",
    "typed-ffmpeg",
]

[project.scripts]
tool-box = "tool_box.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"